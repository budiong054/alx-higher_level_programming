#!/usr/bin/python3
def weight_average(my_list=[]):
    """weight_average - returns the weighted average of
        all integers tuple (<score>, <weight>)
    """
    if not len(my_list):
        return 0
